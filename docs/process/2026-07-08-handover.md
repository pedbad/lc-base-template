# Handover — LC Base Template (2026-07-08)

**Purpose:** Single source of truth to resume work. Full milestone status + task
checklists (done vs todo). Supersedes the point-in-time handovers below for
_status_; those keep their recipes/runbooks as reference.

Related docs:

- `docs/process/2026-07-03-exercise-engines-buildlist.md` — new-engine checklist (canonical for §C)
- `docs/process/2026-07-05-exercise-scaffold-adoption-handover.md` — M1 recipe (DONE)
- `docs/process/2026-07-02-instructions-box-handover.md` — instructions box
- `docs/TOOLING.md` — build-log (findings H1, M1)
- `docs/specs/2026-07-03-new-exercise-engines-design.md` — flashcards/conjugation/reading design

---

## ⚠️ 0. Sync state — READ FIRST (multi-machine)

**This machine (2026-07-08):** `main` == `origin/main`, working tree clean, 0 unpushed
commits. Latest on `main`: `8873a7d` (`refactor: use shared createExerciseReducer in
inline-gap and text-entry`).

**Known risk:** work has been committed on a _separate work machine_ and may not be
pushed. So tomorrow the two machines can be out of sync.

**Before starting work on ANY machine tomorrow:**

```bash
git fetch origin
git status                 # am I ahead / behind origin/main?
# if the OTHER machine has unpushed commits:
git checkout -b feat/<desc>   # branch them off first (don't work straight on main)
git push -u origin feat/<desc>
git checkout main && git pull --ff-only
```

If a machine is _behind_: `git pull --rebase origin main`. If _ahead_ with local
commits: branch + push + PR, never force-push `main`.

---

## 1. Milestone status (overview)

| Milestone         | Scope                                                                                               | Status                                     |
| ----------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| **Phase A**       | Foundation: config + Zod schemas, tokens, `lazyRegistry`, showcase, CI (`.github/workflows/ci.yml`) | ✅ done                                    |
| **Phase B**       | All 12 core exercise engines ported + registered + tested                                           | ✅ done                                    |
| **Finding H1**    | Per-engine pure grading extracted → `*-grading.ts` (+ unit tests)                                   | ✅ done (PRs #1, #3)                       |
| **Finding M1**    | Shared `lib/exerciseScaffold.ts` + adoption across engines                                          | ✅ done (PR #2 + `feat/scaffold-adoption`) |
| **New engine 1a** | Flashcards — Step 1 (in-memory deck)                                                                | ✅ done                                    |
| **New engine 1b** | Flashcards — Step 2 (SRS / Leitner + localStorage)                                                  | ⬜ todo                                    |
| **New engine 2**  | Conjugation table                                                                                   | ⬜ todo                                    |
| **New engine 3**  | Reading comprehension                                                                               | ⬜ todo                                    |
| **Phase C**       | Example LO (`lo-config/`), static per-LO pre-render, full 8-guard suite                             | ⬜ not started                             |

Test suite: `bun test` green (0 fail; ~313 tests). Registry: 13 types wired
(12 core + flashcards). 13 colocated `*.fixture.ts`.

---

## 2. Core engines (Phase B) — 12/12 ✅

All registered in `src/exercises/lazyRegistry.ts`, each with a colocated schema,
pure logic file (grading/deck/sequence/model), `.test.ts`, instructions copy, and
showcase fixture.

| #   | Engine            | Registered | Pure-logic file                  | Notes                                                                  |
| --- | ----------------- | ---------- | -------------------------------- | ---------------------------------------------------------------------- |
| 1   | `select`          | ✅         | `select-grading.ts`              | uses `useExerciseScaffold`                                             |
| 2   | `inline-choice`   | ✅         | `inline-choice-grading.ts`       | uses `useExerciseScaffold`                                             |
| 3   | `radio-quiz`      | ✅         | `radio-quiz-grading.ts`          | uses `useExerciseScaffold`; single-correct                             |
| 4   | `inline-gap`      | ✅         | `inline-gap-grading.ts`          | shared `createExerciseReducer`                                         |
| 5   | `typed-transform` | ✅         | (shared `text-entry-grading.ts`) | thin wrapper over `TextEntryRuntime`, `strict` mode                    |
| 6   | `dictation`       | ✅         | (shared `text-entry-grading.ts`) | wrapper, `dictation` mode; every row needs audio                       |
| 7   | `line-match`      | ✅         | `line-match-grading.ts`          | dual layout (select ≤980 / SVG connectors ≥980); `useExerciseScaffold` |
| 8   | `word-spot`       | ✅         | pure model + scoring             | own model                                                              |
| 9   | `memory-match`    | ✅         | pure deck logic                  | own model (card-flip game)                                             |
| 10  | `word-order`      | ✅         | pure sequence logic              | sequence/placement family                                              |
| 11  | `phrase-reorder`  | ✅         | pure sequence logic              | sequence family                                                        |
| 12  | `drag-fill-gaps`  | ✅         | pure grading                     | sequence/placement family                                              |

`typed-transform` + `dictation` intentionally have no own grading file — grading lives
in the shared `text-entry/text-entry-grading.ts` (`gradeTextEntry`, `fillAnswers`).

**Audio subsystem** (shared, `src/audio/` + `src/components/audio/`): `AudioManager`
singleton, `useAudioClip`, `AudioClip` (native + circular variants),
`SequenceAudioController` (master playlist player), `CircularAudioProgressAnimatedSpeakerDisplay`.
UI refined 2026-07-08: master player dropped volume (play + scrubber + `m:ss` readout);
circular icon uses the shared play/pause glyph (no speaker+arcs).

---

## 3. Refactor findings — H1 + M1 ✅ (both complete)

### H1 — per-engine grading extraction ✅

Grading lifted out of each `*.tsx` into a colocated `*-grading.ts` (pure, unit-tested).
11 engines carry a grading/pure-logic file. Component = wiring + render only.
Merged via PRs #1 + #3.

### M1 — shared exercise scaffold ✅

`src/exercises/lib/exerciseScaffold.ts` (+ 10 tests). Exports `seedFromId`,
`createExerciseReducer` (merge-patch reducer with null-patch no-op),
`useExerciseScaffold`. Adopted in all 4 seed engines (`select` `0f1dd20`,
`inline-choice` `5b3f263`, `radio-quiz` `1beecba`, `line-match` `238011b`);
`inline-gap` + `text-entry` use the shared `createExerciseReducer` (`8873a7d`).
**Local `seedFromId` defs remaining: 0.** Merged via PR #2 + `feat/scaffold-adoption`.

Nothing outstanding on H1 or M1.

---

## 4. New engine roadmap — task checklists

Source: `docs/process/2026-07-03-exercise-engines-buildlist.md` (canonical) and
`docs/specs/2026-07-03-new-exercise-engines-design.md`. Legend: `[x]` done · `[ ]` todo.

### 4.1 Flashcards — Step 1 (in-memory deck) ✅ DONE

- [x] `exercise-types.ts` — `flashcards` key
- [x] `flashcards/flashcards-schema.ts` — Zod (`cards[{target,native,image?,audio?}]`, `options{shuffle?,direction?,lockDirection?,srs?}`, `instructionsField`)
- [x] `flashcards/FlashcardsExercise.tsx` — flip, self-rate (Again/Good), advance, progress, restart (thin view over pure `flashcards-deck.ts`)
- [x] Direction toggle (default target→native, learner-flippable; `lockDirection` hides it)
- [x] Audio icon on `target` field (reuse `AudioClip` / circular speaker)
- [x] `lang={TARGET_LANG}` on target content only
- [x] `lazyRegistry.ts` register · `instructions.ts` copy
- [x] `flashcards.fixture.ts` — cards + placeholder audio (`.m4a`)
- [x] Tests + tsc clean (15 flashcards tests: schema + pure deck reducer)
- [x] Showcase a11y verify — keyboard flip/rate, audio label, lang tagging (2026-07-08)

### 4.2 Flashcards — Step 2 (SRS layer) ⬜ TODO ← **next up**

- [ ] `localStorage` Leitner scheduler (3–5 boxes), keyed per LO+exercise
- [ ] Due-ordering on load; wrong resurfaces, known fades
- [ ] "Reset progress" control
- [ ] Graceful fallback: storage absent/corrupt/cleared → fresh deck
- [ ] Tests for scheduler logic (pure functions) + storage edge cases

### 4.3 Conjugation table ⬜ TODO (build SECOND)

- [ ] `exercise-types.ts` — `conjugation` key
- [ ] `conjugation/conjugation-schema.ts` — `{verb,tense?,prompt?,rows[{person,answer}]}` + `instructionsField`
- [ ] `conjugation/ConjugationExercise.tsx` — grid, typed input per form, blank-grading + standard footer
- [ ] Reuse inline-gap typed-answer normalization (accents)
- [ ] `lang={TARGET_LANG}` on verb/forms
- [ ] `lazyRegistry.ts` · `instructions.ts` · showcase fixture
- [ ] Tests + tsc + a11y verify

### 4.4 Reading comprehension ⬜ TODO (build THIRD)

- [ ] `exercise-types.ts` — `reading` key
- [ ] `reading/reading-schema.ts` — `{passage,questions[{type,prompt,options?,answer}]}` + `instructionsField`
- [ ] `reading/ReadingExercise.tsx` — passage + stacked questions, reuse radio-quiz question unit, aggregate blank-grading
- [ ] v1 = MCQ + true/false only (short-answer parked)
- [ ] `lang={TARGET_LANG}` on passage
- [ ] `lazyRegistry.ts` · `instructions.ts` · showcase fixture
- [ ] Tests + tsc + a11y verify

**Gotcha:** adding a key to `EXERCISE_TYPE_KEYS` before its `EXERCISE_INSTRUCTIONS`
copy is a tsc error by design — add both in the same commit.

---

## 5. Phase C — not started (later)

Per the original spec (`docs/specs/2026-06-15-lc-base-template-design.md`): example LO
folder (`lo-config/`), static per-LO `.html` pre-render (auto-discovered), and the full
8-guard suite (a config-schema · b naming/render-mirror · c asset-path · d asset-existence
· e registry-completeness · f token-integrity · g css-layer-discipline · h w3c/a11y).
Currently only schema validation (guard a) is active; no `lo-config/` yet. Sequence this
after the new engines unless priorities change.

---

## 6. Next up (recommended order)

1. **Flashcards Step 2 (SRS)** — §4.2. Pure Leitner scheduler + localStorage, tests first.
2. **Conjugation table** — §4.3.
3. **Reading comprehension** — §4.4.
4. Then **Phase C** (example LO + static render + remaining guards).

### Continue prompt (paste to resume)

> Resume `lc-base-template`. All 12 core engines + flashcards Step 1 are done, and
> refactor findings H1 (per-engine `*-grading.ts`) + M1 (shared `exerciseScaffold`)
> are complete (see `docs/process/2026-07-08-handover.md`). Next open item is
> **flashcards Step 2 — the SRS layer** (`docs/process/2026-07-03-exercise-engines-buildlist.md`
> §1 Step 2): a pure localStorage Leitner scheduler (3–5 boxes, keyed per LO+exercise),
> due-ordering on load, a "Reset progress" control, and graceful fallback when storage
> is absent/corrupt. Write the pure scheduler + its tests first, then wire it into
> `FlashcardsExercise`. Conventional commits, one concern per commit; run
> `bun test · bun run lint · bun run build` + a11y-verify the showcase before each.
> FIRST: `git fetch origin` and reconcile — a separate work machine may have unpushed
> commits.

---

## 7. Verify / conventions

```bash
bun test            # unit tests (bun:test)
bun run lint        # eslint
bun run lint:css    # stylelint
bun run build       # tsc -b + vite build
bun run dev         # showcase at /exercise-showcase.html (Preview tooling: .claude/launch.json)
```

- Conventional commits; no `Co-Authored-By` (attribution disabled).
- One concern per commit; <800-line file cap; new pure logic ≥80% covered.
- Do not commit `.understand-anything/` (gitignored).
- CI runs test + lint + build on push (`.github/workflows/ci.yml`).
